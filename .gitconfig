[user]
	name = bbonnet
	email = none
[push]
	default = simple
[color]
	ui = auto
[pretty]
	# oneline log format with short hash, relative date, committer name, branch info, short comment
	myformat = "tformat:%C(yellow)%h%Creset %cr %C(blue)%<(7,trunc)%cn%Creset -%C(auto)%d%Creset %s"
[alias]
	co = checkout
	b = branch
	st = status
	s = status -s
	a = add
	ci = commit
	p = push

	l = log --graph --pretty=myformat
	la = log --graph --pretty=myformat --all
	ls = log --graph --pretty=myformat --name-status
	las = log --graph --pretty=myformat --all --name-status
	ld = log --graph --pretty=myformat --date=short

	# minimal word diff
	d = diff --unified=0 --ignore-space-at-eol --color-words='[[:alnum:]]+|[^[:space:]]'
	dc = diff --unified=0 --ignore-space-at-eol --color-words='[[:alnum:]]+|[^[:space:]]' --cached
	dh = diff --unified=0 --ignore-space-at-eol --color-words='[[:alnum:]]+|[^[:space:]]' HEAD^!

	pullr = pull --rebase=preserve
[core]
	pager = less -R
[difftool]
	prompt = false
[diff]
	tool = meld
[merge]
	tool = meld
[mergetool]
	keepBackup = false
	keepTemporaries = false
