# git clone https://github.com/bastien-bonnet/home
# shopt -s dotglob
# rsync -nvihurlt home/* .
# same command without -n

# Move data SSD to new PC, or rsync SSH its data

# FF conf:
# ff install NoScript, uBlock Origin, TST, Improve Youtube
# Bookmark toolbar: right click on bar >  always show. Then right clicke > customize toolbar > merge bookmark with address bar. Then remove bookmark toolbar
# ff restore TST tabs, ie copy recovery.jsonlz4 to $FF_PROFILE/sessionstore.jsonlz4
# Sync: connect (bb)
# ff conf TST gist (Drive)
# ff cron script backup
# ff settings > Open previous… + warn me when closing multiple tab + ctrl+tab cycles + browser layout > uncheck show sidebar
# Term: use nerd font

# Virtual desktops
# KDE Power management balanced icon taskbar

# LUKS other disk(s) (cf. bookmark)

# If AMD, blacklist kvm & kvm_amd modules for Virtualbox
# uvc.bash autostart

# 1. Probably already installed
firefox
git
vim
vim-gtk (for +clipboard option)

# 2. 
conky-all
terminator
virtualbox
virtualbox-guest-additions-iso
guvcview
uvcdynctrl
meld

# 3. Still needed?
source-highlight
lm-sensors
tree
curl
(nvidia-current)


# 4. Document things
asciidoc
pandoc
kile
teensy-loader-cli
texlive-lang-french
texlive-pictures
texlive-latex-extra # Contains Lastpage…
texlive-extra-utils #Contains pdfbook2, for pdf to booklet/brochure

